# @vellora/cli

Versions and release notes are managed with [Changesets](../../.changeset); this file is updated
automatically on release (`changeset version`).

## Unreleased

- Initial public prerelease.
- Add `vellora doctor` for native/Chromium fidelity reports, optional pixel diffs, and policy
  suggestions.
- Add `vellora fidelity --config` to validate committed render-engine policy files.
- Extend `vellora render` inputs with `--engine`, `--template-id`, `--policy`, `--base-url`, `--image`,
  and `--font`.
