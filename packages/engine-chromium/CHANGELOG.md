# @vellora/engine-chromium

Versions and release notes are managed with [Changesets](../../.changeset); this file is updated
automatically on release (`changeset version`).

## Unreleased

- Initial public prerelease.
- Optional browser-fidelity bridge for templates that must render through a Chromium/Chrome
  executable. The main `vellora` package remains browserless by default.
